package game.view.menuview;

import javax.swing.*;
import java.awt.*;

public class MenuViewField extends JPanel {

    @Override
    public void paintComponent(Graphics g) {
        super.paintComponent(g);
        Graphics2D g2d = (Graphics2D) g;
    }
}
